- Parser / Rust tooling / Lynette wrapper context / `vstd` retrieval -> [`lynette-and-vstd.md`](lynette-and-vstd.md).
- Project-level hands-off pilot harness (`tools/verusage_hands_off/`, `runs/verusage_hands_off/`, `docs/verusage_hands_off/`) — third-party agent (`opencode`) drives a stripped benchmark end-to-end, with cost/transcript/validation captured -> [`verusage-hands-off-pilot.md`](verusage-hands-off-pilot.md).
- VeruSAGE+ full-repo harness — pivoted to forking each VeruSAGE upstream project, stripping benchmark proofs in place, and validating with `cargo verus verify`. Migrated out of this repo on 2026-05-16 to the standalone [`VeruLaw`](https://github.com/SichangHe/VeruLaw) repo (typically cloned to `../VeruLaw/`); this repo now only provides the `benchmarks/VeruSAGE-Bench/` dataset that VeruLaw's strip step reads.
